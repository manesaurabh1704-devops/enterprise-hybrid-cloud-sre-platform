apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: platform-api
  labels:
    app: platform-api
spec:
  type: ClusterIP
  selector:
    app: platform-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
